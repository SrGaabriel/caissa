use crate::board::{BitBoard, BitPosition, Team};

pub fn mask_sliding_rays(position: &BitPosition, sliders: &BitBoard, team: Team, directions: &[i8]) -> BitBoard {
    todo!("Implement me!")
}

fn file_attack_mask(bits: &BitBoard) -> BitBoard {
    todo!("Implement me!")
}